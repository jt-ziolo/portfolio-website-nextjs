type SiteConfig = {
  topLevelDomain: string;
  titleTemplate: string;
  titleDefault: string;
  description: string;
};

export const siteConfig: SiteConfig = {
  topLevelDomain: "https://ziolojt.com",
  titleTemplate: "%s | JT Ziolo",
  titleDefault: "JT Ziolo — Full-Stack Web Development Services",
  description: "...",
};
